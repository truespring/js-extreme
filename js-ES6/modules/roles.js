const DEFAULT_ROLE = 'User';
const ADMIN = 'Admin';
export {DEFAULT_ROLE as USER, ADMIN};

// 이 모듈에서 두 개의 상수를 생성한 다음 이들을 중괄호로 감싸 명명된 내보내기로 내보낸다.
// 중괄호로 감쌌다고 해서 객체를 내보낸 것으로 생각하면 안 된다.
// 3번 라인에서 보다시피 export에서 이름을 변경할 수 있다. 또한 import에서도 이름을 변경할 수 있다.
